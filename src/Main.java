import Agents.Launcher;
public class Main {
    public static void main(String[] args) {
        // Launches the JavaFX application (which runs the start method in Launcher)
        Launcher.main(args);
    }
}
